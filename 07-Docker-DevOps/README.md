# 07-Docker-DevOps

Docker & MySQL backup learning notes (Bangla).

| File | Description |
|---|---|
| `docker_tutorial_bangla.md` | Complete Docker tutorial: install (Windows/Linux/macOS), images, containers, Dockerfile, Compose, Volumes, Networking, Multi-stage builds, BuildKit, Production + MySQL backup/restore/automation guide (Google Drive / OneDrive / Task Scheduler) |

> ℹ️ The MySQL-backup section appears twice inside the note (a duplication introduced during
> editing); both copies are identical content. No data was removed.